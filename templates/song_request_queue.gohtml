<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>BB Song Request Queue</title>
</head>
<body>

    <h2>Now Playing: {{ .NowPlaying.SongTitle }} - {{ range .NowPlaying.SongArtists }} {{ . }} {{ end }} <a href="{{ .NowPlaying.SongLink }}"/>Link</a></h2>
    <ul>
    {{ range $index, $item := .CurrentQueue }}
    <li>
        <strong>{{ $item.SongTitle }} - {{ range $item.SongArtists }} {{ . }} {{ end }}</strong>({{$item.Service}}) - <a href="{{ .SongLink }}"/>Link</a> - {{ $item.User }} - Duration: {{ $item.Duration }} {{ $item.Added }}
        <a href="/play_request?id={{ $index }}">Play Now</a>
        <a href="/remove_request?id={{ $index }}">Remove</a>
    </li>
    {{ end }}
    </ul>

    <ul>
    {{ range .History }}
    <li> 
        {{ .SongTitle }} - {{ range .SongArtists }} {{ . }} {{ end }}({{.Service}}) - <a href="{{ .SongLink }}"/>Link</a> - {{ .User }}
    </li>
    {{ end }}
    </ul>
</body>
</html>
